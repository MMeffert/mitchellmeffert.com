---
phase: 03-lightbox-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - js/custom.js
  - css/magnific-popup.css
  - js/jquery.magnific-popup.min.js
autonomous: true

must_haves:
  truths:
    - "Clicking portfolio image opens full-size in lightbox overlay"
    - "Lightbox supports pinch-to-zoom on mobile"
    - "Lightbox navigates between images with arrow keys"
    - "Magnific Popup JS/CSS files removed from project"
  artifacts:
    - path: "index.html"
      provides: "PhotoSwipe CDN links and portfolio gallery structure"
      contains: "photoswipe@5.4.4"
    - path: "js/custom.js"
      provides: "PhotoSwipe initialization replacing Magnific Popup"
      contains: "PhotoSwipeLightbox"
  key_links:
    - from: "index.html"
      to: "PhotoSwipe CDN"
      via: "script and link tags"
      pattern: "cdn\\.jsdelivr\\.net/npm/photoswipe"
    - from: "js/custom.js"
      to: "portfolio-gallery"
      via: "PhotoSwipeLightbox initialization"
      pattern: "gallery.*portfolio-gallery"
---

<objective>
Replace Magnific Popup lightbox with PhotoSwipe for portfolio images

Purpose: Eliminate jQuery-dependent Magnific Popup (deprecated since 2016) in favor of PhotoSwipe (vanilla JS, same author, mobile-optimized with pinch-to-zoom)

Output: Working lightbox for 4 portfolio images with keyboard navigation and mobile gestures
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-lightbox-migration/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PhotoSwipe CDN and restructure portfolio HTML</name>
  <files>index.html</files>
  <action>
  1. In `<head>`, replace Magnific Popup CSS with PhotoSwipe CSS:
     - Remove: `<link rel="stylesheet" href="css/magnific-popup.css">`
     - Add: `<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/photoswipe.css">`

  2. Restructure portfolio gallery (lines ~535-584):
     - Add `pswp-gallery` class and `id="portfolio-gallery"` to the container div (`<div class="row mt-5 work-filter">`)
     - For each portfolio item, restructure the link/image:
       - Remove the empty `<a class="img-zoom">` element
       - Wrap the `<div class="work_images">` with `<a>` tag
       - Add required PhotoSwipe attributes: `data-pswp-width="800"` and `data-pswp-height="800"` (all images are 800x600)
       - Add `target="_blank"` for fallback behavior

  Target HTML structure for each item:
  ```html
  <div class="col-lg-4 webdesign">
      <div class="work_img_box rounded">
          <a href="images/works/rakegenetics.jpg"
             data-pswp-width="800"
             data-pswp-height="600"
             target="_blank">
              <div class="work_images">
                  <img src="images/works/rakegenetics.jpg" alt="Rake Genetics" class="img-fluid mx-auto d-block">
                  <div class="work_overlay">
                      <h4 class="mb-0">Web Design</h4>
                      <h6 class="mb-0">Rake Genetics</h6>
                  </div>
              </div>
          </a>
      </div>
  </div>
  ```

  3. In `<body>` before `</body>`, replace Magnific Popup JS with PhotoSwipe JS:
     - Remove: `<script src="js/jquery.magnific-popup.min.js"></script>`
     - Add before custom.js:
       ```html
       <script src="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/umd/photoswipe.umd.min.js"></script>
       <script src="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/umd/photoswipe-lightbox.umd.min.js"></script>
       ```
  </action>
  <verify>
  - `grep -q "photoswipe@5.4.4" index.html` returns 0
  - `grep -q "magnific-popup" index.html` returns non-zero (removed)
  - `grep -q "portfolio-gallery" index.html` returns 0
  - `grep -c "data-pswp-width" index.html` returns 4 (one per portfolio item)
  </verify>
  <done>PhotoSwipe CDN loaded, Magnific Popup CSS/JS references removed, all 4 portfolio images have required data attributes</done>
</task>

<task type="auto">
  <name>Task 2: Replace Magnific Popup init with PhotoSwipe and cleanup</name>
  <files>js/custom.js, css/magnific-popup.css, js/jquery.magnific-popup.min.js</files>
  <action>
  1. In js/custom.js, replace `initMfpImages` function (lines ~120-131):
     - Replace the entire function body with PhotoSwipe initialization:
     ```javascript
     //PhotoSwipe Lightbox
     ElvishApp.prototype.initMfpImages = function() {
         var lightbox = new PhotoSwipeLightbox({
             gallery: '#portfolio-gallery',
             children: 'a',
             pswpModule: PhotoSwipe,
             bgOpacity: 0.9,
             loop: true,
             arrowKeys: true,
             escKey: true,
             pinchToClose: true,
             closeOnVerticalDrag: true,
             trapFocus: true,
             returnFocus: true,
             preload: [1, 2]
         });
         lightbox.init();
     },
     ```
     Note: Keep function name `initMfpImages` to avoid changing the `init()` call list.

  2. Remove `initMfpVideo` function (lines ~148-157) and its call in `init()`:
     - The video popup section is commented out in HTML (Services section)
     - This function would error after Magnific Popup removal
     - Remove `this.initMfpVideo();` from the `init` function

  3. Delete Magnific Popup files:
     - `rm css/magnific-popup.css`
     - `rm js/jquery.magnific-popup.min.js`
  </action>
  <verify>
  - `grep -q "PhotoSwipeLightbox" js/custom.js` returns 0
  - `grep -q "magnificPopup" js/custom.js` returns non-zero (removed)
  - `grep -q "initMfpVideo" js/custom.js` returns non-zero (removed)
  - `ls css/magnific-popup.css 2>/dev/null` returns non-zero (file deleted)
  - `ls js/jquery.magnific-popup.min.js 2>/dev/null` returns non-zero (file deleted)
  </verify>
  <done>PhotoSwipe initialization replaces Magnific Popup, video popup function removed, Magnific Popup files deleted</done>
</task>

</tasks>

<verification>
1. Open index.html in browser
2. Navigate to "My Work" section
3. Click on any portfolio image - should open in PhotoSwipe lightbox overlay
4. Test keyboard navigation: arrow keys to navigate, Escape to close
5. Test on mobile (or Chrome DevTools mobile mode): pinch-to-zoom should work
6. No console errors related to Magnific Popup or missing files
7. Filter buttons still work (Isotope unaffected)
</verification>

<success_criteria>
- [ ] Clicking portfolio image opens full-size in lightbox overlay
- [ ] Lightbox supports pinch-to-zoom on mobile (test in DevTools mobile mode)
- [ ] Lightbox navigates between images with arrow keys
- [ ] Magnific Popup JS/CSS files removed from project
- [ ] No JavaScript console errors
- [ ] Portfolio filter buttons still function correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-lightbox-migration/03-01-SUMMARY.md`
</output>
