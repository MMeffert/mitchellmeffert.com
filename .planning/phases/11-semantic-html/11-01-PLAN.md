---
phase: 11-semantic-html
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Screen reader announces social link purposes (e.g., 'Facebook profile')"
    - "Screen reader encounters exactly one h1 identifying page purpose"
    - "Heading hierarchy is logical with no skipped levels"
  artifacts:
    - path: "index.html"
      provides: "Accessible icon links and proper heading structure"
      contains: "aria-label"
  key_links:
    - from: "icon links"
      to: "screen reader"
      via: "aria-label attribute"
      pattern: "aria-label=\".*\""
---

<objective>
Add aria-labels to icon-only links and fix heading hierarchy so screen readers can understand interactive elements and page structure.

Purpose: Address A11Y-03 (icon-only links need aria-labels) and A11Y-05 (single h1 heading)
Output: index.html with accessible icon links and proper heading hierarchy
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aria-labels to icon-only links</name>
  <files>index.html</files>
  <action>
Add aria-label attributes to all icon-only links:

Header social icons (lines 155-162):
- Facebook: aria-label="Facebook profile"
- LinkedIn: aria-label="LinkedIn profile"
- Instagram: aria-label="Instagram profile"
- Reddit: aria-label="Reddit profile"
- YouTube: aria-label="YouTube channel"
- StackOverflow: aria-label="Stack Overflow profile"
- GitHub: aria-label="GitHub profile"
- Shutterstock: aria-label="Shutterstock portfolio"

Footer social icons (lines 768-775):
- Same aria-labels as header icons

Scroll down arrow (lines 174-176):
- aria-label="Scroll down to about section"

Back to top button (line 785):
- aria-label="Back to top"

Pattern: `<a href="..." aria-label="[description]">`
  </action>
  <verify>grep -c "aria-label" index.html should return 18 (8 header + 8 footer + 1 scroll + 1 back-to-top)</verify>
  <done>All icon-only links have descriptive aria-label attributes</done>
</task>

<task type="auto">
  <name>Task 2: Fix heading hierarchy</name>
  <files>index.html</files>
  <action>
Change duplicate h1 elements to h2 while keeping exactly one h1:

KEEP as h1 (line 153):
- `<h1 class="header_title mb-0 mt-3">I am...` - This is the main page heading

CHANGE to h2:
- Line 237: `<h1 class="lan_fun_value mb-1"` -> `<h2 class="lan_fun_value mb-1"`
- Line 246: `<h1 class="lan_fun_value mb-1"` -> `<h2 class="lan_fun_value mb-1"`
- Line 255: `<h1 class="lan_fun_value mb-1"` -> `<h2 class="lan_fun_value mb-1"`
- Line 264: `<h1 class="lan_fun_value mb-1"` -> `<h2 class="lan_fun_value mb-1"`
- Line 390: `<h1>I Am Available` -> `<h2>I Am Available`

Note: The closing tags must also change from `</h1>` to `</h2>` for these elements.

Verify existing h2 elements (section titles) remain unchanged - these form proper hierarchy under the h1.
  </action>
  <verify>grep -c "<h1" index.html should return 1; grep -c "<h2" index.html should return count including the 5 newly changed plus existing h2s</verify>
  <done>Page has exactly one h1 element; former h1s are now h2s maintaining logical hierarchy</done>
</task>

</tasks>

<verification>
1. `grep -c "aria-label" index.html` returns 18
2. `grep -c "<h1" index.html` returns 1
3. `grep "<h1" index.html` shows only the hero heading "I am..."
4. Open in browser, use VoiceOver/NVDA to verify:
   - Social icons announce their purpose
   - Heading navigation shows single h1 followed by h2s
</verification>

<success_criteria>
- All 18 icon-only links have aria-label attributes
- Exactly one h1 element remains (hero section)
- Heading hierarchy is h1 > h2 with no skipped levels
- No accessibility warnings for unlabeled links
</success_criteria>

<output>
After completion, create `.planning/phases/11-semantic-html/11-01-SUMMARY.md`
</output>
