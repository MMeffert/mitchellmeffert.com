---
phase: 11-semantic-html
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "Screen reader announces 'main' region when entering main content"
    - "Screen reader announces 'footer' region at page end"
    - "User can jump between landmarks using screen reader navigation"
  artifacts:
    - path: "index.html"
      provides: "Semantic landmark elements"
      contains: "<main"
  key_links:
    - from: "landmark elements"
      to: "screen reader"
      via: "semantic HTML"
      pattern: "<main|<footer|role=\"contentinfo\""
---

<objective>
Add semantic landmark elements (main, footer) so screen reader users can navigate page structure efficiently.

Purpose: Address A11Y-04 (semantic landmarks for screen reader navigation)
Output: index.html with proper main and footer landmark regions
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add main landmark</name>
  <files>index.html</files>
  <action>
Wrap the main content sections in a `<main>` element with an aria-label:

Insert `<main aria-label="Main content">` AFTER the home/hero section closing tag (after line 179: `<!--END HOME-->`).

Close the main element `</main>` BEFORE the footer section (before line 757: `<!-- START FOOTER -->`).

The main landmark should contain:
- About section
- Funfacts section
- Education & Experience section
- CTA section
- Work section
- Client testimonials section
- Certifications section
- Blog section
- Contact form section

The main landmark should NOT contain:
- Home/hero section (it's more of a banner)
- Footer section (separate landmark)
- Back to top link (page-level utility)

Note: Do NOT wrap the hero section - it serves as a banner/header region, not main content.
  </action>
  <verify>grep -c "<main" index.html returns 1; grep "<main" index.html shows aria-label attribute</verify>
  <done>Main content wrapped in semantic main element with aria-label</done>
</task>

<task type="auto">
  <name>Task 2: Convert footer section to semantic footer</name>
  <files>index.html</files>
  <action>
Convert the footer section (lines 757-782) from a `<section>` to a `<footer>` element:

BEFORE (line 758):
`<section class="bg-light">`

AFTER:
`<footer class="bg-light" aria-label="Site footer">`

Also change the closing tag (line 781):
BEFORE: `</section>`
AFTER: `</footer>`

The footer element is a semantic landmark (role="contentinfo" is implicit).
Adding aria-label="Site footer" provides additional context for screen readers.

Keep all content and classes unchanged - only the element type changes.
  </action>
  <verify>grep -c "<footer" index.html returns 1; grep "</footer>" index.html returns 1</verify>
  <done>Footer uses semantic footer element with aria-label</done>
</task>

</tasks>

<verification>
1. `grep "<main" index.html` shows `<main aria-label="Main content">`
2. `grep "</main>" index.html` shows closing tag before footer
3. `grep "<footer" index.html` shows `<footer class="bg-light" aria-label="Site footer">`
4. Open in browser, use VoiceOver/NVDA landmarks navigation:
   - "Main" region is announced
   - "Footer" or "Content info" region is announced
   - Can jump between landmarks
</verification>

<success_criteria>
- Page has `<main>` element wrapping content sections
- Page has `<footer>` element (not `<section>`) for footer
- Both landmarks have aria-labels for clarity
- Screen reader can navigate between landmarks
- No duplicate landmarks (one main, one footer)
</success_criteria>

<output>
After completion, create `.planning/phases/11-semantic-html/11-02-SUMMARY.md`
</output>
