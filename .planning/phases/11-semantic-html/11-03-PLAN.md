---
phase: 11-semantic-html
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Heading hierarchy follows h1 > h2 > h3 > h4 with no skipped levels"
    - "Screen reader heading navigation shows logical document structure"
    - "Decorative text uses visual styling classes, not heading elements"
  artifacts:
    - path: "index.html"
      provides: "Proper heading hierarchy"
      contains: "h3"
  key_links:
    - from: "heading elements"
      to: "screen reader"
      via: "semantic heading levels"
      pattern: "h[1-6]"
---

<objective>
Fix heading hierarchy gaps identified in verification. Address skipped heading levels throughout the document so screen reader users can navigate the page structure logically.

Purpose: Close Gap 2 from 11-VERIFICATION.md (heading hierarchy has skipped levels)
Output: index.html with proper h1 > h2 > h3 > h4 hierarchy, no skipped levels

Note on Gap 1 (nav landmark): This gap is NOT addressed because it is not applicable. This is a single-page scrolling portfolio with no traditional navigation menu. The social media links in the hero section are supplemental content (already have aria-labels), not primary navigation. WCAG does not require a nav landmark when there is no navigation menu. The existing main + footer landmarks are appropriate for this page type.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-semantic-html/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix decorative text using heading element</name>
  <files>index.html</files>
  <action>
Line 152 has `<h4>Hello & Welcome</h4>` appearing BEFORE the h1. This is decorative/introductory text, not a structural heading.

Change line 152:
FROM: `<h4>Hello & Welcome</h4>`
TO: `<p class="h4">Hello & Welcome</p>`

The `h4` Bootstrap class preserves the visual styling while removing the heading semantics. This text introduces the hero section but is not a document structure heading.
  </action>
  <verify>grep -n "Hello & Welcome" index.html shows `<p class="h4">` not `<h4>`</verify>
  <done>Decorative hero text no longer uses heading element; h1 is first true heading</done>
</task>

<task type="auto">
  <name>Task 2: Fix Education section heading levels (h2 > h4 skip)</name>
  <files>index.html</files>
  <action>
The Education & Experience section has h2 ("Education & Experience") jumping directly to h4 for subsection titles, skipping h3.

Change all h4 elements in the Education section to h3:

Line 296: `<h4>Studied at Madison College</h4>` -> `<h3>Studied at Madison College</h3>`
Line 308: `<h4>Studied at University of Wisconsin - Whitewater</h4>` -> `<h3>Studied at University of Wisconsin - Whitewater</h3>`
Line 320: `<h4>Studied at Waunakee High School</h4>` -> `<h3>Studied at Waunakee High School</h3>`
Line 335: `<h4>Web Developer/IT Lead</h4>` -> `<h3>Web Developer/IT Lead</h3>`
Line 347: `<h4>Systems Analyst III</h4>` -> `<h3>Systems Analyst III</h3>` (Cerner)
Line 359: `<h4>Systems Analyst III</h4>` -> `<h3>Systems Analyst III</h3>` (Siemens)
Line 371: `<h4>Web Team Coordinator/Virtualization Engineer</h4>` -> `<h3>Web Team Coordinator/Virtualization Engineer</h3>`

This creates proper hierarchy: h2 (section) > h3 (items)
  </action>
  <verify>grep -c "<h4>" index.html in education section should return 0; grep -c "<h3>" index.html should show increased count</verify>
  <done>Education section follows h2 > h3 hierarchy with no skipped levels</done>
</task>

<task type="auto">
  <name>Task 3: Fix Work section heading levels (h4 > h6 skip)</name>
  <files>index.html</files>
  <action>
The Work section has h4 ("Web Design", "Python Lambda Function") and h6 (project names) creating a skip.

Change the Work section portfolio item headings:

Lines with h4 "Web Design" or similar category headings -> h3
Lines with h6 project names -> h4

Specifically in the work_overlay divs (around lines 436-485):

Line 436: `<h4 class="mb-0">Web Design</h4>` -> `<h3 class="mb-0">Web Design</h3>`
Line 437: `<h6 class="mb-0">Rake Genetics</h6>` -> `<h4 class="mb-0">Rake Genetics</h4>`

Line 452: `<h4>Web Design</h4>` -> `<h3>Web Design</h3>`
Line 453: `<h6>Dane County New Teacher Project</h6>` -> `<h4>Dane County New Teacher Project</h4>`

Line 468: `<h4>Web Design</h4>` -> `<h3>Web Design</h3>`
Line 469: `<h6>Roundhouse Marketing</h6>` -> `<h4>Roundhouse Marketing</h4>`

Line 484: `<h4>Python Lambda Function</h4>` -> `<h3>Python Lambda Function</h3>`
Line 485: `<h6>Contact form lambda written in Python</h6>` -> `<h4>Contact form lambda written in Python</h4>`

This creates proper hierarchy: h2 (My Work section) > h3 (category) > h4 (project name)
  </action>
  <verify>grep -c "<h6>" index.html should return 0 (no h6 elements remain); Work section has h3 and h4 elements</verify>
  <done>Work section follows h2 > h3 > h4 hierarchy with no skipped levels</done>
</task>

<task type="auto">
  <name>Task 4: Fix Blog section heading levels (h2 > h5 skip)</name>
  <files>index.html</files>
  <action>
The Blog section has h2 ("My Small Business Blog") jumping directly to h5 for blog post titles, skipping h3 and h4.

Change all h5 blog post title elements to h3:

Line 651: `<h5 class="fw-bold mb-0"><a href="...">2008 iMac Upgrade to SSD</a></h5>` -> `<h3 class="fw-bold mb-0"><a href="...">2008 iMac Upgrade to SSD</a></h3>`

Line 667: `<h5 class="fw-bold mb-0"><a href="...">pfSense Aliases</a></h5>` -> `<h3 class="fw-bold mb-0"><a href="...">pfSense Aliases</a></h3>`

Line 683: `<h5 class="fw-bold mb-0"><a href="...">Cisco SG300-28P for Homelab</a></h5>` -> `<h3 class="fw-bold mb-0"><a href="...">Cisco SG300-28P for Homelab</a></h3>`

This creates proper hierarchy: h2 (Blog section) > h3 (blog post titles)
  </action>
  <verify>grep -c "<h5" index.html in blog section should return 0; blog titles should use h3</verify>
  <done>Blog section follows h2 > h3 hierarchy with no skipped levels</done>
</task>

</tasks>

<verification>
1. Heading level audit - run: `grep -n "<h[1-6]" index.html | head -30`
   - Should show h1 at line 153 (hero)
   - h2 for section titles (About, Education, Work, etc.)
   - h3 for subsection items (education entries, work items, blog titles)
   - h4 only where h3 exists above it (work project names)
   - No h5 or h6 elements

2. No skipped levels check:
   - After each h2, next heading should be h2 or h3 (never h4, h5, h6)
   - After each h3, next heading should be h2, h3, or h4 (never h5, h6)

3. Decorative text check:
   - `grep "Hello & Welcome" index.html` should show `<p class="h4">` not `<h4>`

4. Screen reader test (human verification if available):
   - Use VoiceOver heading navigation (VO+Command+H)
   - Headings should appear in logical order without "skipping" announcements
</verification>

<success_criteria>
- No h5 or h6 elements in document (except if genuinely needed for 5th-level nesting)
- "Hello & Welcome" uses `<p class="h4">` not `<h4>`
- Education section: h2 > h3 (no h4)
- Work section: h2 > h3 > h4 (no h6)
- Blog section: h2 > h3 (no h5)
- Screen reader heading navigation shows logical document structure
</success_criteria>

<output>
After completion, create `.planning/phases/11-semantic-html/11-03-SUMMARY.md`
</output>
