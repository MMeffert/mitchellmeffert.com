---
phase: 10-accessibility-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [css/style.css]
autonomous: true

must_haves:
  truths:
    - "All body text meets WCAG 4.5:1 contrast ratio against its background"
    - "All interactive element text (links, buttons) meets WCAG 4.5:1 contrast ratio"
    - "Muted/secondary text is still readable with sufficient contrast"
  artifacts:
    - path: "css/style.css"
      provides: "WCAG-compliant color values in custom properties"
      contains: "--color-text-muted"
  key_links:
    - from: ":root custom properties"
      to: "text color declarations"
      via: "var() references"
      pattern: "var\\(--color-text"
---

<objective>
Fix color contrast failures to meet WCAG 4.5:1 minimum

Purpose: Users with low vision or color blindness (accessibility requirement A11Y-02) must be able to read all text content. Current muted text colors (#9a9a9a, #999, #9c9c9c) have only ~2.85:1 contrast against white backgrounds, failing the WCAG AA minimum of 4.5:1.
Output: Updated CSS custom property values that provide 4.5:1+ contrast while maintaining visual hierarchy.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@css/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update contrast-failing custom properties</name>
  <files>css/style.css</files>
  <action>
Update the following CSS custom properties in the :root block (lines 41-87) to meet WCAG 4.5:1 contrast against white (#fff):

Current values that FAIL:
- `--color-text-light: #9a9a9a` (2.85:1) -> change to `#767676` (4.54:1)
- `--color-tag-text: #9c9c9c` (2.81:1) -> change to `#767676` (4.54:1)

Current values that are BORDERLINE (improve for safety):
- `--color-text-muted: #777` (4.48:1) -> change to `#757575` (4.6:1)
- `--color-pagination: #777777` (4.48:1) -> change to `#757575` (4.6:1)
- `--color-dot: #757575` (4.6:1) -> already compliant, keep as is

Values that are FINE (do not change):
- `--color-text-description: #666` (5.74:1) - compliant
- `--color-text: #000` (21:1) - compliant
- `--color-text-input: #4c5667` (7.1:1) - compliant
- `--color-border-social: #868e96` (3.54:1) - this is for borders, not text
- `--color-text-separator: #bbb` (2.16:1) - this is decorative, not readable text

The updated :root block text section should be:
```css
    /* Text */
    --color-text: #000;
    --color-text-muted: #757575;
    --color-text-description: #666;
    --color-text-light: #767676;
    --color-text-separator: #bbb;
    --color-text-input: #4c5667;
```

And in the Special section:
```css
    --color-tag-text: #767676;
    --color-pagination: #757575;
```

Contrast ratios for reference (against white #fff):
- #767676 = 4.54:1 (passes WCAG AA for normal text)
- #757575 = 4.6:1 (passes WCAG AA for normal text)
- #666666 = 5.74:1 (passes WCAG AA)
  </action>
  <verify>
1. Confirm custom property values:
   - `grep "color-text-light" css/style.css` shows #767676
   - `grep "color-text-muted" css/style.css` shows #757575
   - `grep "color-tag-text" css/style.css` shows #767676
   - `grep "color-pagination" css/style.css` shows #757575

2. Visual check in browser:
   - Filter menu text (work section) is readable
   - Form labels and muted text are readable
   - Tag text in blog section is readable
   - Pagination numbers are readable
  </verify>
  <done>
All text colors now meet WCAG 4.5:1 minimum contrast ratio:
- --color-text-light: #767676 (4.54:1)
- --color-text-muted: #757575 (4.6:1)
- --color-tag-text: #767676 (4.54:1)
- --color-pagination: #757575 (4.6:1)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all text color usages</name>
  <files>css/style.css</files>
  <action>
Search for any hardcoded color values that might bypass the custom properties and fail contrast:

1. Search for hardcoded gray colors: `grep -n "#9\|#8\|#7\|rgba" css/style.css`
2. Look for any text-related rules using colors that are not custom properties

If any hardcoded colors found in text-related rules, update them to use the appropriate custom property:
- Gray text should use `var(--color-text-muted)` or `var(--color-text-light)`
- Description text should use `var(--color-text-description)`

Known acceptable hardcoded values (do not change):
- rgba() values for transparency effects (overlays, shadows)
- Border colors (not text)
- Background colors

Phase 9 already converted colors to custom properties, so this task is primarily verification. Only make changes if you find hardcoded text colors that were missed.
  </action>
  <verify>
1. `grep -n "color:.*#[0-9a-f]" css/style.css` - should return very few results
2. Any results should be non-text colors (borders, decorative elements) or already compliant values
3. All text color declarations use var(--color-*) references
  </verify>
  <done>
No hardcoded text colors bypass the custom properties. All text colors flow through the :root variables which now meet WCAG 4.5:1 contrast.
  </done>
</task>

</tasks>

<verification>
1. Contrast ratio verification:
   - All custom property values for text meet 4.5:1 minimum
   - #767676 on #ffffff = 4.54:1 (PASS)
   - #757575 on #ffffff = 4.6:1 (PASS)
   - #666666 on #ffffff = 5.74:1 (PASS)

2. Visual regression check:
   - Site still has visual hierarchy (primary vs muted text)
   - Muted text is readable but clearly secondary
   - No dramatic visual changes (colors are similar but darker)

3. Component check:
   - Portfolio filter menu text is readable
   - Recaptcha notice text is readable
   - Blog tag text is readable
   - Pagination text is readable
</verification>

<success_criteria>
- All text-related custom properties meet WCAG 4.5:1 contrast ratio
- No hardcoded gray text colors bypass the custom properties
- Visual hierarchy is maintained (muted text still appears secondary)
- Text is clearly readable in all sections
</success_criteria>

<output>
After completion, create `.planning/phases/10-accessibility-core/10-02-SUMMARY.md`
</output>
