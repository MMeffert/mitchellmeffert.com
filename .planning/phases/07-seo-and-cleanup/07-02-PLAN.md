---
phase: 07-seo-and-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - js/bootstrap.min.js
  - js/popper.min.js
autonomous: true

must_haves:
  truths:
    - "No commented-out HTML blocks in index.html"
    - "No unused JS files in assets folder"
    - "Site still functions after cleanup"
  artifacts:
    - path: "index.html"
      provides: "Clean HTML without dead code"
  key_links:
    - from: "index.html"
      to: "js/bootstrap.bundle.min.js"
      via: "script src"
      pattern: "bootstrap\\.bundle\\.min\\.js"
---

<objective>
Remove unused files and dead code from the codebase after modernization.

Purpose: Reduce bundle size (71KB) and eliminate technical debt from Bootstrap 4 to 5 migration.
Output: Clean codebase with only actively used files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seo-and-cleanup/07-RESEARCH.md

Reference files:
@index.html (lines 224-308 - commented Services section)
@js/ directory contents
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused JavaScript files</name>
  <files>js/bootstrap.min.js, js/popper.min.js</files>
  <action>
Delete the two unused JavaScript files identified in Phase 5 verification:

1. Delete `js/bootstrap.min.js` (51KB)
   - This is the Bootstrap 4 JS file
   - Site now uses `js/bootstrap.bundle.min.js` (Bootstrap 5)
   - Not referenced anywhere in index.html

2. Delete `js/popper.min.js` (20KB)
   - This is standalone Popper.js v1
   - Bootstrap 5 bundle includes Popper v2
   - Not referenced anywhere in index.html

Before deletion, verify these files are NOT referenced in index.html:
- `grep "bootstrap.min.js" index.html` should return nothing
- `grep "popper.min.js" index.html` should return nothing

Total savings: 71KB of dead code removed.
  </action>
  <verify>
Run `ls js/` - should show only: bootstrap.bundle.min.js, custom.js (2 files)
Run `grep -E "bootstrap\.min\.js|popper\.min\.js" index.html` - should return nothing (no matches)
  </verify>
  <done>
js/ directory contains only actively used files. 71KB of unused files removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove commented-out Services section</name>
  <files>index.html</files>
  <action>
Remove the commented-out Services section from index.html (lines 223-309):

The section spans from:
```html
        <!-- START SERVICES -->
        <!--<section class="section bg-light" id="services">
```

To:
```html
        </section> -->
        <!-- END SERVICES -->
```

This is 85+ lines of dead HTML that:
- Contains placeholder Lorem Ipsum text
- Is not displayed (commented out)
- References a feature not used on this portfolio site

Remove ONLY the commented-out content. Keep:
- The `<!-- END ABOUT -->` comment above it (line 221)
- The `<!-- END FUNFACTS -->` comment below it (line 311)

The result should be clean HTML with About section flowing directly into Funfacts section.
  </action>
  <verify>
Run `grep -c "START SERVICES" index.html` - should return 0
Run `grep -c "id=\"services\"" index.html` - should return 0
Run `wc -l index.html` - should be ~803 lines (was 889, removed ~86)
  </verify>
  <done>
No commented-out HTML blocks remain in index.html. Services placeholder removed.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Run `ls js/` - expect: bootstrap.bundle.min.js, custom.js (2 files only)
2. Run `grep -E "bootstrap\.min\.js|popper\.min\.js" index.html` - expect no matches
3. Run `grep "START SERVICES" index.html` - expect no matches
4. Open site in browser - verify all functionality still works:
   - Testimonial carousel rotates
   - Portfolio lightbox opens images
   - Portfolio filters work
   - Contact form submits (can test with dev tools network tab)
   - Back to top button works
</verification>

<success_criteria>
- js/ directory contains only 2 files: bootstrap.bundle.min.js, custom.js
- index.html has no commented-out sections
- Site loads without console errors
- All existing functionality works (carousel, lightbox, filters, contact form)
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-and-cleanup/07-02-SUMMARY.md`
</output>
