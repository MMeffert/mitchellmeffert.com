---
phase: 01-bootstrap-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/bootstrap.min.css
  - js/bootstrap.bundle.min.js
  - index.html
  - css/style.css
autonomous: false

must_haves:
  truths:
    - "Bootstrap 5.3.8 CSS loads without console errors"
    - "Bootstrap 5.3.8 JS loads without console errors"
    - "All sections display with correct spacing and alignment"
    - "No deprecated Bootstrap 4 class names remain in HTML"
    - "Link underlines render correctly (no unexpected underlines)"
  artifacts:
    - path: "css/bootstrap.min.css"
      provides: "Bootstrap 5.3.8 CSS framework"
      contains: "Bootstrap v5.3"
    - path: "js/bootstrap.bundle.min.js"
      provides: "Bootstrap 5.3.8 JS with Popper bundled"
      contains: "Bootstrap v5.3"
    - path: "index.html"
      provides: "HTML with Bootstrap 5 class names"
      min_lines: 800
    - path: "css/style.css"
      provides: "Custom CSS with Bootstrap 5 selectors"
      contains: "fw-bold"
  key_links:
    - from: "index.html"
      to: "css/bootstrap.min.css"
      via: "link href"
      pattern: "bootstrap\\.min\\.css"
    - from: "index.html"
      to: "js/bootstrap.bundle.min.js"
      via: "script src"
      pattern: "bootstrap\\.bundle\\.min\\.js"
---

<objective>
Upgrade Bootstrap 4 to Bootstrap 5.3.8 and update all CSS class names.

Purpose: This is the foundational migration that unlocks all subsequent modernization work. Bootstrap 5 removes jQuery dependency (though jQuery remains for plugins this phase), uses logical properties for RTL support (ml/mr to ms/me), and provides modern CSS custom properties for future theming.

Output: Site renders identically to current state but running on Bootstrap 5.3.8.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-migration/01-RESEARCH.md
@index.html
@css/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Bootstrap CSS and JS files</name>
  <files>
    css/bootstrap.min.css
    js/bootstrap.bundle.min.js
    index.html
  </files>
  <action>
    1. Download Bootstrap 5.3.8 CSS:
       - Fetch from: https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css
       - Save to: css/bootstrap.min.css (overwrite existing)

    2. Download Bootstrap 5.3.8 Bundle JS:
       - Fetch from: https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js
       - Save to: js/bootstrap.bundle.min.js (NEW file - includes Popper)

    3. Update script references in index.html (around line 874-876):
       - Remove the popper.min.js script tag: `<script src="js/popper.min.js"></script>`
       - Change `<script src="js/bootstrap.min.js"></script>`
         to: `<script src="js/bootstrap.bundle.min.js"></script>`

    IMPORTANT: Keep jQuery loaded - plugins still depend on it.
    The old js/bootstrap.min.js and js/popper.min.js files can remain on disk (cleanup in later phase).
  </action>
  <verify>
    - css/bootstrap.min.css contains "Bootstrap v5.3"
    - js/bootstrap.bundle.min.js contains "Bootstrap v5.3"
    - index.html has no reference to popper.min.js
    - index.html references bootstrap.bundle.min.js
    - index.html still references jquery.min.js
  </verify>
  <done>
    Bootstrap 5.3.8 CSS and Bundle JS are installed and referenced in index.html.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Bootstrap 4 class names to Bootstrap 5</name>
  <files>
    index.html
    css/style.css
  </files>
  <action>
    **In index.html - perform these find/replace operations:**

    1. Replace `font-weight-bold` with `fw-bold` (12 occurrences)
       - Lines: 142, 175, 185, 199, 206, 213, 224, 231, 238, 391, 405, 417, 429, 444, 456, 468, 480, 519, 598, 613, 623, 633, 643, 653, 671, 732, 736, 748, 764, 784, 800, 806, 812, 820
       - Note: Some are in commented sections - update ALL occurrences for consistency

    2. Replace `mr-0` with `me-0` (3 occurrences)
       - Lines 187, 188, 189 (About section job titles)

    3. Replace `pl-2 pr-2` with `ps-2 pe-2` (2 occurrences)
       - Lines 735, 751 (Blog card descriptions)

    4. Replace `text-right` with `text-end` (1 occurrence)
       - Line 826 (Contact form submit button wrapper)

    5. Replace `float-left` with `float-start` (1 occurrence)
       - Line 849 (Footer copyright)

    6. Replace `float-right` with `float-end` (1 occurrence)
       - Line 852 (Footer social icons)

    **In css/style.css - update CSS selectors:**

    1. Lines 55-62: Change heading selectors from `.font-weight-bold` to `.fw-bold`:
       ```css
       h1.fw-bold,
       h2.fw-bold,
       h3.fw-bold,
       h4.fw-bold,
       h5.fw-bold,
       h6.fw-bold {
           font-weight: 600 !important;
       }
       ```

    2. Lines 1420-1424: Change float selectors:
       ```css
       .float_none.float-end,
       .float_none.float-start {
           float: none !important;
           text-align: center;
       }
       ```

    Note: Link underline fix already exists at line 69 (`text-decoration: none !important`).
  </action>
  <verify>
    - `grep -c "font-weight-bold" index.html` returns 0 (or only in comments)
    - `grep -c "fw-bold" index.html` returns 12+ matches
    - `grep -c "mr-0" index.html` returns 0
    - `grep -c "me-0" index.html` returns 3
    - `grep -c "pl-2" index.html` returns 0
    - `grep -c "text-right" index.html` returns 0
    - `grep -c "float-left" index.html` returns 0
    - `grep -c "float-right" index.html` returns 0
    - css/style.css contains "h1.fw-bold"
    - css/style.css contains ".float_none.float-end"
  </verify>
  <done>
    All Bootstrap 4 class names updated to Bootstrap 5 equivalents in both index.html and style.css.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Bootstrap migration</name>
  <what-built>
    Bootstrap 5.3.8 CSS/JS files installed and all class names migrated.
  </what-built>
  <how-to-verify>
    1. Open index.html in a browser (can use `open index.html` or local server)
    2. Open browser console (F12 or Cmd+Option+I) and check for:
       - No 404 errors for CSS/JS files
       - No JavaScript errors
       - No Bootstrap-related console warnings

    3. Visual regression check - verify each section renders correctly:
       - [ ] Hero section: Typed.js animation, social icons display inline
       - [ ] About section: Text alignment, job titles inline with separators
       - [ ] Skills section: Progress bars display correctly
       - [ ] Fun Facts: Counter boxes evenly spaced
       - [ ] Education/Experience: Timeline layout, year badges styled
       - [ ] CTA section: Button centered and styled
       - [ ] Portfolio/Work: Filter buttons, image grid
       - [ ] Testimonials: Carousel auto-rotates (Owl Carousel still works)
       - [ ] Certifications: Images centered in grid
       - [ ] Blog cards: Correct padding (ps-2/pe-2)
       - [ ] Contact form: Submit button right-aligned (text-end working)
       - [ ] Footer: Copyright left, social icons right (float-start/end working)

    4. Mobile responsive check (resize browser or use DevTools):
       - Footer elements stack and center on mobile (float_none working)
       - All sections readable on mobile widths

    5. Verify no unexpected link underlines anywhere
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
**Phase 1 Success Criteria Check:**

1. Bootstrap 5.3.8 CSS and JS load without console errors
   - Verify: Browser console shows no 404s, no JS errors

2. Navigation hamburger menu opens/closes on mobile
   - NOTE: Research confirmed this site has NO hamburger menu (single-page scroll design)
   - This criterion is NOT APPLICABLE

3. All sections display with correct spacing and alignment
   - Verify: Visual inspection of all page sections

4. No deprecated `data-toggle` attributes remain
   - NOTE: Research confirmed this site uses NO Bootstrap data attributes
   - Verify: `grep -c "data-toggle" index.html` returns 0

5. Link underlines render correctly
   - NOTE: Already handled by existing `text-decoration: none !important` in style.css
   - Verify: Visual inspection shows no unexpected underlines
</verification>

<success_criteria>
- Bootstrap 5.3.8 CSS/JS files are in place and loaded
- All 22+ class name occurrences updated (fw-bold, me-0, ps-2/pe-2, text-end, float-start/end)
- CSS selectors in style.css updated to match new class names
- Page renders identically to pre-migration state
- No console errors
- jQuery and all plugins (Owl Carousel, Magnific Popup, Isotope) continue working
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-migration/01-01-SUMMARY.md`
</output>
