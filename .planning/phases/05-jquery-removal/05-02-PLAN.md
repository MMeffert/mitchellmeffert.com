---
phase: 05-jquery-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/custom.js
  - index.html
  - css/style.css
autonomous: true

must_haves:
  truths:
    - "Preloader fades out on page load"
    - "Navbar becomes sticky after scrolling 50px"
    - "Smooth scroll navigation works for all anchor links"
    - "Navigation highlights current section on scroll (scrollspy)"
    - "Fun facts counter animates when scrolled into view"
    - "Back to top button appears after scrolling and works on click"
    - "Hero typing animation cycles through all strings"
  artifacts:
    - path: "js/custom.js"
      provides: "Vanilla JS ElvishApp without jQuery"
      min_lines: 120
    - path: "index.html"
      provides: "Typed.js v2.1.0 CDN and Bootstrap ScrollSpy data attributes"
      contains: "typed.js@2.1.0"
    - path: "css/style.css"
      provides: "CSS for smooth scrolling and fade transitions"
      contains: "scroll-behavior: smooth"
  key_links:
    - from: "index.html body"
      to: "Bootstrap ScrollSpy"
      via: "data-bs-spy attribute"
      pattern: "data-bs-spy=\"scroll\""
    - from: "index.html"
      to: "Typed.js v2.1.0"
      via: "CDN script tag"
      pattern: "cdn.jsdelivr.net/npm/typed.js@2.1.0"
---

<objective>
Convert custom.js from jQuery to vanilla JavaScript and update Typed.js to modern version

Purpose: Remove all jQuery dependencies from custom.js (8 functions), replace old jQuery-based typed.js with modern vanilla JS version, use Bootstrap 5's native ScrollSpy
Output: custom.js works without jQuery, hero typing animation uses Typed.js v2.1.0
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jquery-removal/05-RESEARCH.md
@js/custom.js (current jQuery-based implementation)
@index.html (script tags and body structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert custom.js to vanilla JavaScript</name>
  <files>js/custom.js</files>
  <action>
Rewrite custom.js to pure vanilla JavaScript. Convert each function:

**1. initPreLoader:**
```javascript
initPreLoader: function() {
    var status = document.getElementById('status');
    var preloader = document.getElementById('preloader');

    // Fade out status
    status.style.opacity = '0';

    setTimeout(function() {
        status.style.display = 'none';
        preloader.style.opacity = '0';

        setTimeout(function() {
            preloader.style.display = 'none';
            document.body.style.overflow = 'visible';
        }, 350);
    }, 350);
}
```

**2. initNavbarStickey:**
```javascript
initNavbarStickey: function() {
    window.addEventListener('scroll', function() {
        var sticky = document.querySelector('.sticky');
        if (!sticky) return;

        if (window.scrollY >= 50) {
            sticky.classList.add('stickyadd');
        } else {
            sticky.classList.remove('stickyadd');
        }
    });
}
```

**3. initNavbarSmooth:**
```javascript
initNavbarSmooth: function() {
    document.querySelectorAll('.navbar-nav a, .scroll_down a').forEach(function(link) {
        link.addEventListener('click', function(event) {
            var href = this.getAttribute('href');
            if (href && href.startsWith('#')) {
                event.preventDefault();
                var target = document.querySelector(href);
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            }
        });
    });
}
```

**4. initNavbarScrollSpy:**
```javascript
initNavbarScrollSpy: function() {
    // Bootstrap 5 ScrollSpy initialized via data attributes on body
    // This function can be empty or omitted - Bootstrap handles it
}
```

**5. initFunFacts:**
```javascript
initFunFacts: function() {
    var counted = false;
    var counterSection = document.getElementById('counter');
    if (!counterSection) return;

    function animateValue(element, start, end, duration) {
        var startTime = performance.now();

        function update(currentTime) {
            var elapsed = currentTime - startTime;
            var progress = Math.min(elapsed / duration, 1);
            // easeOutQuad easing
            var easeProgress = 1 - (1 - progress) * (1 - progress);
            var current = Math.floor(start + (end - start) * easeProgress);
            element.textContent = current;

            if (progress < 1) {
                requestAnimationFrame(update);
            }
        }

        requestAnimationFrame(update);
    }

    window.addEventListener('scroll', function() {
        if (counted) return;

        var rect = counterSection.getBoundingClientRect();
        if (rect.top < window.innerHeight) {
            counted = true;
            document.querySelectorAll('.lan_fun_value').forEach(function(el) {
                var countTo = parseInt(el.getAttribute('data-count'), 10);
                animateValue(el, 0, countTo, 2000);
            });
        }
    });
}
```

**6. initBackToTop:**
```javascript
initBackToTop: function() {
    var backTop = document.querySelector('.back_top');
    if (!backTop) return;

    window.addEventListener('scroll', function() {
        if (window.scrollY > 100) {
            backTop.style.opacity = '1';
            backTop.style.visibility = 'visible';
        } else {
            backTop.style.opacity = '0';
            backTop.style.visibility = 'hidden';
        }
    });

    backTop.addEventListener('click', function(event) {
        event.preventDefault();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    });
}
```

**7. initTypedText:**
```javascript
initTypedText: function() {
    // Typed.js v2.1.0 uses vanilla JS API
    document.querySelectorAll('.element').forEach(function(el) {
        new Typed(el, {
            strings: el.getAttribute('data-elements').split(','),
            typeSpeed: 100,
            backDelay: 3000,
            backSpeed: 50,
            loop: true,
            showCursor: true,
            cursorChar: '|'
        });
    });
}
```

**8. initPortfolioFilter and initMfpImages and initClientSlider:**
Keep these unchanged - they were already converted to vanilla JS in Phases 3 and 4.

**9. Change the IIFE wrapper:**
Replace `!function($) { ... }(window.jQuery)` with:
```javascript
(function() {
    "use strict";

    var ElvishApp = {
        // ... all functions ...

        init: function() {
            this.initPreLoader();
            this.initNavbarStickey();
            this.initNavbarSmooth();
            this.initNavbarScrollSpy();
            this.initFunFacts();
            this.initPortfolioFilter();
            this.initMfpImages();
            this.initClientSlider();
            this.initBackToTop();
            this.initTypedText();
        }
    };

    // Initialize when DOM ready
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', function() {
            ElvishApp.init();
        });
    } else {
        ElvishApp.init();
    }
})();
```

Remove the jQuery plugin assignment at the end (`$.ElvishApp = new ElvishApp`).
  </action>
  <verify>
1. Run `grep -n '\\$' js/custom.js` - should return no matches (no $ symbols except in comments)
2. Run `grep -n 'jQuery' js/custom.js` - should return no matches
3. Open site in browser - preloader should fade, scroll features should work
  </verify>
  <done>
custom.js contains zero jQuery references and all 8 functions use native DOM APIs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update index.html for Typed.js v2.1.0 and Bootstrap ScrollSpy</name>
  <files>index.html</files>
  <action>
1. **Add Bootstrap ScrollSpy data attributes to body tag:**
   Change `<body>` to:
   ```html
   <body data-bs-spy="scroll" data-bs-target="#navbarCollapse" data-bs-root-margin="0px 0px -20%" data-bs-smooth-scroll="true">
   ```
   Note: The site doesn't have a navbar with id="navbarCollapse" visible in the current HTML, but the scrollspy init in custom.js referenced it. If no navbar navigation exists, the scrollspy won't do anything but won't break either.

2. **Replace typed.js with CDN version:**
   Remove this line from script tags:
   ```html
   <script src="js/typed.js"></script>
   ```

   Add before custom.js:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/typed.js@2.1.0/dist/typed.umd.js"></script>
   ```

3. **Remove the inline typed initialization at the bottom:**
   Delete these lines (around 915-922):
   ```javascript
   $(".element").each(function() {
       var $this = $(this);
       $this.typed({
           strings: $this.attr('data-elements').split(','),
           typeSpeed: 100,
           backDelay: 3000
       });
   });
   ```
   Keep only the copyright year line:
   ```javascript
   document.getElementById('copyright-year').textContent = new Date().getFullYear();
   ```

The typed initialization now happens in custom.js initTypedText function.
  </action>
  <verify>
1. Open site - hero typing animation should work ("Mitchell Meffert.", "A Web Developer.", etc.)
2. Check browser console - no errors about undefined Typed
3. Check Network tab - typed.umd.js should load from jsdelivr
  </verify>
  <done>
index.html loads Typed.js v2.1.0 from CDN, inline jQuery typed init removed, Bootstrap ScrollSpy data attributes added to body.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CSS for smooth scrolling and fade transitions</name>
  <files>css/style.css</files>
  <action>
Add CSS at the end of style.css to support vanilla JS transitions:

```css
/* ==========================================================================
   Smooth Scrolling and Transitions (jQuery Removal - Phase 5)
   ========================================================================== */

/* Enable native smooth scrolling */
html {
    scroll-behavior: smooth;
}

/* Account for any fixed header when scrolling to sections */
section[id] {
    scroll-margin-top: 0;
}

/* Transition properties for preloader fade */
#preloader,
#status {
    transition: opacity 0.35s ease;
}

/* Transition for back-to-top button */
.back_top {
    transition: opacity 0.35s ease, visibility 0.35s ease;
    opacity: 0;
    visibility: hidden;
}
```

Note: The existing CSS may already have some of these styles. Check first and only add what's missing. If `.back_top` already has display-based show/hide, the opacity/visibility approach will override it properly since we're using !important is NOT needed when the new JS sets inline styles.
  </action>
  <verify>
1. Open site and scroll - smooth scrolling should work on anchor links
2. Scroll down 100px - back to top button should fade in smoothly
3. Scroll back up - back to top button should fade out smoothly
4. Preloader should fade out smoothly on page load
  </verify>
  <done>
CSS includes smooth scroll-behavior, scroll-margin for sections, and transition properties for preloader and back-to-top button.
  </done>
</task>

</tasks>

<verification>
- [ ] custom.js has no `$` or `jQuery` references
- [ ] Preloader fades out smoothly on page load
- [ ] Sticky navbar appears after scrolling 50px
- [ ] Clicking nav links smooth scrolls to sections
- [ ] Fun facts counter animates when scrolled into view (17, 30, 12, 26)
- [ ] Back to top button fades in/out based on scroll position
- [ ] Hero typing animation works ("Mitchell Meffert.", "A Web Developer.", etc.)
- [ ] Network tab shows typed.umd.js loading from jsdelivr CDN
- [ ] No JavaScript console errors
</verification>

<success_criteria>
All custom.js functions work with vanilla JavaScript. Hero typing animation uses Typed.js v2.1.0 from CDN. No jQuery references remain in custom.js.
</success_criteria>

<output>
After completion, create `.planning/phases/05-jquery-removal/05-02-SUMMARY.md`
</output>
