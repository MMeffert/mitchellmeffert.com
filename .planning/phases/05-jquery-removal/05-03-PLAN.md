---
phase: 05-jquery-removal
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "Site loads and functions without jQuery"
    - "No JavaScript console errors on page load"
    - "All interactive features work (contact form, carousel, lightbox, filter, typed text)"
    - "jQuery library files removed from project"
    - "Bundle size reduced by ~87KB+ (jQuery + plugins)"
  artifacts:
    - path: "index.html"
      provides: "HTML without jQuery script tags"
  key_links:
    - from: "index.html"
      to: "js/custom.js"
      via: "script tag"
      pattern: "<script src=\"js/custom.js\""
---

<objective>
Remove jQuery and deprecated plugins, verify site functions correctly

Purpose: Complete the jQuery removal by removing script tags and verifying all features still work
Output: Site runs jQuery-free with reduced bundle size
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jquery-removal/05-01-SUMMARY.md
@.planning/phases/05-jquery-removal/05-02-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove jQuery and deprecated plugin script tags</name>
  <files>index.html</files>
  <action>
Remove these script tags from index.html (around lines 898-904):

**Remove:**
```html
<script src="js/jquery.min.js"></script>
```

```html
<script src="js/jquery.easing.min.js"></script>
```

```html
<script src="js/scrollspy.min.js"></script>
```

The `js/typed.js` line should already be removed from Plan 02, but verify it's gone.

**Keep these script tags (in this order):**
1. `js/bootstrap.bundle.min.js` - Bootstrap 5 (still needed)
2. PhotoSwipe CDN scripts
3. Isotope CDN script
4. Splide CDN script
5. Typed.js v2.1.0 CDN (added in Plan 02)
6. `js/custom.js` - Our vanilla JS app

**Final script section should look like:**
```html
<!-- JAVASCRIPTS -->
<script src="js/bootstrap.bundle.min.js"></script>
<!-- PhotoSwipe Lightbox -->
<script src="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/umd/photoswipe.umd.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/umd/photoswipe-lightbox.umd.min.js"></script>
<!--PORTFOLIO FILTER JS-->
<script src="https://cdn.jsdelivr.net/npm/isotope-layout@3.0.6/dist/isotope.pkgd.min.js"></script>
<!-- Splide Carousel -->
<script src="https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/js/splide.min.js"></script>
<!-- Typed.js -->
<script src="https://cdn.jsdelivr.net/npm/typed.js@2.1.0/dist/typed.umd.js"></script>
<!--CUSTOM JS-->
<script src="js/custom.js"></script>
<script>
    // Set copyright year
    document.getElementById('copyright-year').textContent = new Date().getFullYear();
</script>
```
  </action>
  <verify>
1. Run `grep -n 'jquery' index.html` - should return no matches (case insensitive check)
2. Run `grep -n 'scrollspy.min.js' index.html` - should return no matches
3. Run `grep -n 'easing' index.html` - should return no matches
  </verify>
  <done>
index.html contains no jQuery, jQuery Easing, or scrollspy.min.js script references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete deprecated JavaScript files</name>
  <files>js/jquery.min.js, js/jquery.easing.min.js, js/scrollspy.min.js, js/typed.js</files>
  <action>
Delete these files from the js/ directory:

```bash
rm js/jquery.min.js
rm js/jquery.easing.min.js
rm js/scrollspy.min.js
rm js/typed.js
```

These files are no longer referenced and can be safely removed:
- `jquery.min.js` (~87KB) - Replaced by native DOM APIs
- `jquery.easing.min.js` (~3KB) - Replaced by CSS scroll-behavior
- `scrollspy.min.js` (~2KB) - Replaced by Bootstrap 5 ScrollSpy
- `typed.js` (~8KB) - Replaced by Typed.js v2.1.0 CDN

Total size reduction: ~100KB
  </action>
  <verify>
1. Run `ls js/` - should only show: bootstrap.bundle.min.js, bootstrap.min.js, contact.js, custom.js, popper.min.js
2. Run `ls js/*.min.js | wc -l` - should return 3 (bootstrap files + popper)
  </verify>
  <done>
Deprecated jQuery-dependent files removed from js/ directory.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full site verification</name>
  <files>index.html, js/custom.js</files>
  <action>
Perform comprehensive verification that all features work without jQuery:

**1. Verify no jQuery references remain in codebase:**
```bash
grep -rn '\$(' index.html js/custom.js
grep -rn 'jQuery' index.html js/custom.js
```
Both should return no results.

**2. Run the site locally and verify each feature:**

Open index.html in browser and test:

a) **Preloader**: Page loads, preloader fades out smoothly
b) **Sticky navbar**: Scroll down 50px, navbar should become sticky (if applicable)
c) **Smooth scroll**: Click any nav link or scroll-down arrow, should smooth scroll
d) **Hero typing**: "I am Mitchell Meffert." should type out, then backspace, then "A Web Developer." etc.
e) **Fun facts counter**: Scroll to stats section, numbers should animate from 0 to their values (17, 30, 12, 26)
f) **Portfolio filter**: Click filter buttons (All, Programming, Webdesign, Photography), items should filter with animation
g) **Lightbox**: Click any portfolio image, should open in PhotoSwipe lightbox
h) **Carousel**: Testimonials section should auto-rotate every 7 seconds
i) **Back to top**: Scroll down, back-to-top button should appear; click it, should scroll to top
j) **Contact form**: Fill out and submit (or test validation by leaving fields empty)

**3. Check browser console:**
- No errors on page load
- No errors during interaction with any feature
- No "$ is not defined" or "jQuery is not defined" errors
  </action>
  <verify>
Open browser DevTools console, refresh page, interact with all features - zero JavaScript errors.
  </verify>
  <done>
Site functions fully without jQuery. All 10 interactive features verified working. Zero console errors.
  </done>
</task>

</tasks>

<verification>
- [ ] No `$` or `jQuery` references in index.html or js/custom.js
- [ ] jQuery files deleted from js/ directory (jquery.min.js, jquery.easing.min.js, scrollspy.min.js, typed.js)
- [ ] Browser console shows zero JavaScript errors
- [ ] Preloader fades out on page load
- [ ] Smooth scrolling works on anchor links
- [ ] Hero typing animation works
- [ ] Fun facts counter animates
- [ ] Portfolio filter works
- [ ] Lightbox opens images
- [ ] Testimonial carousel auto-rotates
- [ ] Back to top button works
- [ ] Contact form submits successfully
</verification>

<success_criteria>
1. Contact form submits successfully (sends email via Lambda) - VERIFIED
2. Contact form shows success/error messages correctly - VERIFIED
3. All custom.js functions work without jQuery (8+ functions converted) - VERIFIED
4. No `$` or `jQuery` references remain in codebase - VERIFIED
5. jQuery library removed from project (~87KB+ eliminated) - VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/05-jquery-removal/05-03-SUMMARY.md`
</output>
