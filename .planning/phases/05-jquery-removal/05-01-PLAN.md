---
phase: 05-jquery-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true

must_haves:
  truths:
    - "Contact form submits successfully and sends email via Lambda"
    - "Contact form shows success message with green text on successful submit"
    - "Contact form shows error message with red text on failed submit"
    - "Form fields clear after successful submission"
    - "reCAPTCHA Enterprise validation works before submission"
  artifacts:
    - path: "index.html"
      provides: "Contact form with native fetch() API"
      contains: "fetch("
  key_links:
    - from: "submitToAPI function"
      to: "Lambda URL"
      via: "fetch() POST request"
      pattern: "fetch.*lambda-url"
---

<objective>
Convert contact form from jQuery $.ajax() to native fetch() API

Purpose: Remove jQuery dependency from contact form while maintaining identical functionality (validation, reCAPTCHA, success/error messaging)
Output: Contact form working with pure vanilla JavaScript
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-jquery-removal/05-RESEARCH.md
@index.html (lines 9-68 - current contact form script)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert contact form to fetch() API</name>
  <files>index.html</files>
  <action>
Replace the inline submitToAPI function (lines 10-67 in head) with vanilla JavaScript version:

1. Replace all jQuery selectors with native DOM:
   - `$("#name").val()` -> `document.getElementById('name').value`
   - `$("#mail").val()` -> `document.getElementById('mail').value`
   - `$("#subject").val()` -> `document.getElementById('subject').value`
   - `$("#comment").val()` -> `document.getElementById('comment').value`
   - `$("#message").css('color', 'green')` -> `document.getElementById('message').style.color = 'green'`
   - `$("#message").text(...)` -> `document.getElementById('message').textContent = ...`

2. Replace $.ajax() with fetch():
   ```javascript
   const response = await fetch(
       'https://txy7g2ztcmlm7klk5ptcv5zze40schil.lambda-url.us-east-1.on.aws/',
       {
           method: 'POST',
           headers: {
               'Content-Type': 'application/json; charset=utf-8'
           },
           body: JSON.stringify(data)
       }
   );

   if (!response.ok) {
       throw new Error('Network response was not ok');
   }
   ```

3. Keep the existing validation logic (name regex, email regex) but update to use native DOM
4. Keep the reCAPTCHA Enterprise flow unchanged (grecaptcha.enterprise.execute still works)
5. Keep the same success/error message display behavior
6. Clear form fields on success using `element.value = ''`

The function should remain async and use try/catch for error handling (already present).
  </action>
  <verify>
1. Open index.html in browser
2. Fill out contact form with valid data
3. Submit and verify:
   - No console errors
   - Success message appears in green
   - Form fields clear after success
4. Test with invalid email - should show alert
5. Test with name < 2 chars - should show alert
  </verify>
  <done>
Contact form submits via fetch() API, shows success/error messages correctly, and clears on success. No jQuery selectors ($) remain in submitToAPI function.
  </done>
</task>

</tasks>

<verification>
- [ ] No `$` or `jQuery` references in submitToAPI function
- [ ] Contact form submits successfully (check Network tab for 200 response)
- [ ] Success message displays with green text
- [ ] Error message displays with red text (can test by temporarily breaking URL)
- [ ] Form fields clear after successful submission
- [ ] Validation alerts still work (name, email)
</verification>

<success_criteria>
Contact form works identically to before but uses native fetch() instead of $.ajax(). The submitToAPI function contains zero jQuery references.
</success_criteria>

<output>
After completion, create `.planning/phases/05-jquery-removal/05-01-SUMMARY.md`
</output>
