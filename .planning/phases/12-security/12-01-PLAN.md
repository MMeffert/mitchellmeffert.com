---
phase: 12-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true

must_haves:
  truths:
    - "CDN-loaded JavaScript files have integrity and crossorigin attributes"
    - "CDN-loaded CSS files have integrity and crossorigin attributes"
    - "Site loads without console errors after SRI implementation"
    - "Browser blocks resource loading if integrity check fails"
  artifacts:
    - path: "index.html"
      provides: "SRI-protected CDN resource references"
      contains: "integrity=\"sha384-"
  key_links:
    - from: "index.html"
      to: "cdn.jsdelivr.net resources"
      via: "integrity and crossorigin attributes"
      pattern: "integrity=\"sha384-.*\" crossorigin=\"anonymous\""
---

<objective>
Add Subresource Integrity (SRI) hashes to all CDN-loaded resources to protect against tampering.

Purpose: Ensure browsers verify CDN resources match expected content before execution, preventing supply-chain attacks via compromised CDN content.

Output: index.html with integrity and crossorigin attributes on all jsDelivr resources (7 total: 2 CSS, 5 JavaScript).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-security/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SRI hashes to CSS resources</name>
  <files>index.html</files>
  <action>
Add integrity and crossorigin="anonymous" attributes to both CDN CSS files in the head section.

PhotoSwipe CSS (line ~118):
```html
<link rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/photoswipe.css"
      integrity="sha384-IfxC36XL/toUyJ939C73PcgMuRzAZuIzZxE38drsmO5p6jD7ei+Zx/1oA/0l8ysE"
      crossorigin="anonymous">
```

Splide CSS (line ~124):
```html
<link rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/css/splide.min.css"
      integrity="sha384-RQYEYzAnHyG0swTqlleCCsKHN9pUKyYMflAk6KEjvY6KhE5MLlvjwZkai5538g2T"
      crossorigin="anonymous">
```

CRITICAL: Both `integrity` AND `crossorigin="anonymous"` are REQUIRED. Without crossorigin, browser silently ignores integrity check ("fails open").
  </action>
  <verify>grep -E 'photoswipe\.css|splide\.min\.css' index.html | grep -c 'integrity.*crossorigin' should return 2</verify>
  <done>Both CSS link elements have integrity hashes and crossorigin attributes</done>
</task>

<task type="auto">
  <name>Task 2: Add SRI hashes to JavaScript resources</name>
  <files>index.html</files>
  <action>
Add integrity and crossorigin="anonymous" attributes to all 5 jsDelivr JavaScript files near end of body (lines ~793-800).

PhotoSwipe Core:
```html
<script src="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/umd/photoswipe.umd.min.js"
        integrity="sha384-k8EKyYcONphQ7zH4cQ0888JapXwrLTXQl/Ue1/jYgjVYahln1NWpnt2S4IC56LNh"
        crossorigin="anonymous"></script>
```

PhotoSwipe Lightbox:
```html
<script src="https://cdn.jsdelivr.net/npm/photoswipe@5.4.4/dist/umd/photoswipe-lightbox.umd.min.js"
        integrity="sha384-IiBVbUz6+U+Tbm/ijO2P0XRwcVzNfrMzloNLkrqHkbi6w5H0v6ie4fI9BIO4SwdK"
        crossorigin="anonymous"></script>
```

Isotope:
```html
<script src="https://cdn.jsdelivr.net/npm/isotope-layout@3.0.6/dist/isotope.pkgd.min.js"
        integrity="sha384-vtH+5pZsjdWxaTWlFSCrWM6i0TIG0HKOqJbPo91LB35dvWpVzuWdJeVoNweP+eoY"
        crossorigin="anonymous"></script>
```

Splide JS:
```html
<script src="https://cdn.jsdelivr.net/npm/@splidejs/splide@4.1.4/dist/js/splide.min.js"
        integrity="sha384-RbYB5yr9jD1p+2OHlV3KzOBLAY48CbFsJ87bXEFYINGgOlQJNE4cEjTUf+Q2fygb"
        crossorigin="anonymous"></script>
```

Typed.js:
```html
<script src="https://cdn.jsdelivr.net/npm/typed.js@2.1.0/dist/typed.umd.js"
        integrity="sha384-cMrTlShXEGSdSFA359p+3aVUxK/R+0TAfbRZMcTlAn8yqzxEDj05QsS65nTFMMj4"
        crossorigin="anonymous"></script>
```

DO NOT add SRI to Google reCAPTCHA - it uses dynamic content and cannot support SRI (documented limitation).

CRITICAL: Both `integrity` AND `crossorigin="anonymous"` are REQUIRED on each script tag.
  </action>
  <verify>grep -E 'cdn\.jsdelivr\.net.*\.js' index.html | grep -c 'integrity.*crossorigin' should return 5</verify>
  <done>All 5 JavaScript script elements have integrity hashes and crossorigin attributes</done>
</task>

<task type="auto">
  <name>Task 3: Verify SRI implementation with local testing</name>
  <files>index.html</files>
  <action>
Start local server and verify site loads correctly:

1. Start local HTTP server:
   ```bash
   cd /Users/mitchellmeffert/Git/Personal/mitchellmeffert.com && python3 -m http.server 8080 &
   ```

2. Open in browser and test functionality:
   - Home page loads without errors
   - Carousel (Splide) works - can navigate slides
   - Portfolio filter (Isotope) works - can filter by category
   - Lightbox (PhotoSwipe) works - can click portfolio images
   - Typing effect (Typed.js) works - hero text animates

3. Check browser console for any SRI-related errors:
   - No "Failed to find a valid digest" errors
   - No CORS errors
   - All CDN resources load successfully

4. OPTIONAL validation: Temporarily corrupt one hash character, refresh, confirm browser blocks the resource (proves SRI is active), then restore correct hash.

5. Stop local server when done.
  </action>
  <verify>Site loads, all JavaScript functionality works (carousel, filter, lightbox, typing effect), no console errors</verify>
  <done>SRI implementation verified working - all 7 CDN resources protected</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Count SRI-protected resources:
   ```bash
   grep -c 'integrity="sha384-' index.html
   ```
   Expected: 7 (2 CSS + 5 JavaScript)

2. Verify all have crossorigin:
   ```bash
   grep 'integrity="sha384-' index.html | grep -c 'crossorigin="anonymous"'
   ```
   Expected: 7

3. Verify Google reCAPTCHA unchanged (no SRI):
   ```bash
   grep 'recaptcha/enterprise.js' index.html | grep -v integrity
   ```
   Expected: 1 match (no integrity attribute)
</verification>

<success_criteria>
- All 7 jsDelivr resources have integrity="sha384-..." attributes
- All 7 jsDelivr resources have crossorigin="anonymous" attributes
- Google reCAPTCHA script has NO integrity attribute (documented limitation)
- Site loads without console errors
- All JavaScript functionality works (carousel, filter, lightbox, typing)
- SEC-02 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-security/12-01-SUMMARY.md`
</output>
