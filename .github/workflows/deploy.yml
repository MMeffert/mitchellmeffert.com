name: Deploy to AWS

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write # Required for OIDC authentication
  contents: read

env:
  AWS_REGION: us-east-1

jobs:
  deploy:
    name: Deploy Static Site
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get CloudFormation outputs
        id: cfn
        run: |
          # Get S3 bucket name from CloudFormation stack outputs
          BUCKET_NAME=$(aws cloudformation describe-stacks \
            --stack-name mitchellmeffert-website-stack \
            --query "Stacks[0].Outputs[?OutputKey=='S3BucketName'].OutputValue" \
            --output text)
          echo "bucket_name=$BUCKET_NAME" >> $GITHUB_OUTPUT

          # Get CloudFront distribution ID
          DISTRIBUTION_ID=$(aws cloudformation describe-stacks \
            --stack-name mitchellmeffert-website-stack \
            --query "Stacks[0].Outputs[?OutputKey=='CloudFrontDistributionId'].OutputValue" \
            --output text)
          echo "distribution_id=$DISTRIBUTION_ID" >> $GITHUB_OUTPUT

      - name: Stamp sitemap lastmod with deploy date
        run: |
          # Keep <lastmod> current so search engines see the page as freshly updated
          sed -i "s|<lastmod>.*</lastmod>|<lastmod>$(date -u +%Y-%m-%d)</lastmod>|" sitemap.xml
          grep lastmod sitemap.xml

      - name: Sync files to S3
        run: |
          # Sync all static files to S3, excluding non-site files
          aws s3 sync . s3://${{ steps.cfn.outputs.bucket_name }} \
            --delete \
            --exclude ".git/*" \
            --exclude ".github/*" \
            --exclude ".claude/*" \
            --exclude ".planning/*" \
            --exclude "cdk/*" \
            --exclude ".gitattributes" \
            --exclude ".gitignore" \
            --exclude "CLAUDE.md" \
            --exclude "SECURITY.md" \
            --cache-control "max-age=31536000,public" \
            --metadata-directive REPLACE

          # Set shorter cache for HTML files (they change more frequently)
          aws s3 cp s3://${{ steps.cfn.outputs.bucket_name }}/index.html \
            s3://${{ steps.cfn.outputs.bucket_name }}/index.html \
            --cache-control "max-age=300,public" \
            --content-type "text/html" \
            --metadata-directive REPLACE

          # Mutable text assets have no content hash in their filenames (and the
          # CloudFront function strips query strings, defeating ?v= cache-busting),
          # so give them a short cache. Otherwise the blanket 1-year cache above
          # would keep returning visitors / crawlers on stale CSS, JS, and SEO files.
          aws s3 cp s3://${{ steps.cfn.outputs.bucket_name }}/css/style.css \
            s3://${{ steps.cfn.outputs.bucket_name }}/css/style.css \
            --cache-control "max-age=3600,public" \
            --content-type "text/css" \
            --metadata-directive REPLACE

          aws s3 cp s3://${{ steps.cfn.outputs.bucket_name }}/js/custom.js \
            s3://${{ steps.cfn.outputs.bucket_name }}/js/custom.js \
            --cache-control "max-age=3600,public" \
            --content-type "application/javascript" \
            --metadata-directive REPLACE

          aws s3 cp s3://${{ steps.cfn.outputs.bucket_name }}/sitemap.xml \
            s3://${{ steps.cfn.outputs.bucket_name }}/sitemap.xml \
            --cache-control "max-age=3600,public" \
            --content-type "application/xml" \
            --metadata-directive REPLACE

          aws s3 cp s3://${{ steps.cfn.outputs.bucket_name }}/robots.txt \
            s3://${{ steps.cfn.outputs.bucket_name }}/robots.txt \
            --cache-control "max-age=3600,public" \
            --content-type "text/plain" \
            --metadata-directive REPLACE

      - name: Clean up stale files from S3
        run: |
          aws s3 rm s3://${{ steps.cfn.outputs.bucket_name }}/.planning --recursive || true
          aws s3 rm s3://${{ steps.cfn.outputs.bucket_name }}/.github --recursive || true
          aws s3 rm s3://${{ steps.cfn.outputs.bucket_name }}/.claude --recursive || true
          aws s3 rm s3://${{ steps.cfn.outputs.bucket_name }}/cdk --recursive || true
          aws s3 rm s3://${{ steps.cfn.outputs.bucket_name }}/.gitignore || true
          aws s3 rm s3://${{ steps.cfn.outputs.bucket_name }}/.gitattributes || true
          aws s3 rm s3://${{ steps.cfn.outputs.bucket_name }}/CLAUDE.md || true
          aws s3 rm s3://${{ steps.cfn.outputs.bucket_name }}/SECURITY.md || true

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ steps.cfn.outputs.distribution_id }} \
            --paths "/*"

      - name: Deployment complete
        run: |
          echo "Deployment complete!"
          echo "Website URL: https://mitchellmeffert.com"
          echo "CloudFront Distribution: ${{ steps.cfn.outputs.distribution_id }}"
